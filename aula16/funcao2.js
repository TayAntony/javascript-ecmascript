function soma(n1=0, n2=0) {
    return n1+n2
}

console.log(soma(2)) // ao nao passar um dos parâmetros o código retorna NaN (not a number), para resolver isso é possível colocar o parâmetro como 0 ('opcional')